# 全仓统一静态分析配置。
# analyzer 会从被分析文件所在目录逐级向上查找本文件，因此各 package 无需各写一份。
include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # 打印到 stdout 会绕过导出门禁，直接判死
    avoid_print: error
    # 动态调用会绕过类型系统，在加密/合并逻辑里等于关掉安全网
    avoid_dynamic_calls: error
    # 测试专用 API 泄漏到 lib/ 是发布事故
    invalid_use_of_visible_for_testing_member: error
    invalid_use_of_protected_member: error
    deprecated_member_use_from_same_package: warning
    todo: ignore
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/generated_plugin_registrant.dart"
    - "**/l10n/generated/**"
    - "build/**"

# Dart 3.7+ 的 formatter 配置（`dart format` 读取此段）
formatter:
  page_width: 100

linter:
  rules:
    # 关闭 comment_references。
    #
    # 这条规则想抓「文档注释里引用了不存在的名字」，但它的实现只能在
    # **本库的 import 范围内**解析名字。本项目大量文档注释引用的是
    # 导出包里的类型（如 pf_crypto 的库注释引用 pf_core 的 Argon2Params），
    # 那些名字通过 export 对使用方可见，对分析器不可见 —— 于是全部误报。
    #
    # 为了消掉误报而加一堆只为文档存在的 import，会让 import 区失去信息量；
    # 而误报长期存在又会训练人忽略整个分析输出。两害相权，关掉这条。
    # 替代的防错手段是 review 约定：文档注释里引用的类型必须能在同包内解析。
    comment_references: false
    # ---- 类型安全 ----
    avoid_dynamic_calls: true
    cast_nullable_to_non_nullable: true
    tighten_type_of_initializing_formals: true
    unnecessary_null_checks: true
    unnecessary_nullable_for_final_variable_declarations: true
    use_if_null_to_convert_nulls_to_bools: true

    # ---- 健全性 / 正确性 ----
    avoid_empty_else: true
    avoid_js_rounded_ints: true
    avoid_null_checks_in_equality_operators: true
    avoid_shadowing_type_parameters: true
    avoid_slow_async_io: true
    avoid_type_to_string: true
    avoid_types_as_parameter_names: true
    await_only_futures: true
    cancel_subscriptions: true
    close_sinks: true
    collection_methods_unrelated_type: true
    control_flow_in_finally: true
    empty_catches: true
    exhaustive_cases: true
    literal_only_boolean_expressions: true
    no_default_cases: true
    no_duplicate_case_values: true
    no_logic_in_create_state: true
    no_runtimeType_toString: true
    null_check_on_nullable_type_parameter: true
    recursive_getters: true
    type_literal_in_constant_pattern: true
    unawaited_futures: true
    unnecessary_statements: true
    unrelated_type_equality_checks: true
    valid_regexps: true
    void_checks: true

    # ---- 安全相关（本项目的重点，非默认套装）----
    avoid_catching_errors: true
    avoid_implementing_value_types: true
    avoid_returning_null_for_future: true
    avoid_void_async: true
    only_throw_errors: true
    use_rethrow_when_possible: true

    # ---- 文档与 API 表面 ----
    always_declare_return_types: true
    # comment_references 在 linter 段顶部显式关闭，原因见那里的说明。
    dangling_library_doc_comments: true
    library_names: true
    library_prefixes: true
    package_names: true
    provide_deprecation_message: true
    slash_for_doc_comments: true
    type_annotate_public_apis: true

    # ---- 导入 ----
    avoid_relative_lib_imports: true
    depend_on_referenced_packages: true
    directives_ordering: true
    implementation_imports: true
    prefer_relative_imports: false

    # ---- 风格（克制地开，避免与 formatter 打架）----
    camel_case_extensions: true
    camel_case_types: true
    curly_braces_in_flow_control_structures: true
    eol_at_end_of_file: true
    implicit_call_tearoffs: true
    non_constant_identifier_names: true
    no_adjacent_strings_in_list: true
    no_leading_underscores_for_local_identifiers: true
    noop_primitive_operations: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_final_fields: true
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_if_null_operators: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    prefer_is_not_operator: true
    prefer_null_aware_operators: true
    prefer_single_quotes: true
    prefer_typing_uninitialized_variables: true
    sort_constructors_first: true
    sort_pub_dependencies: true
    sort_unnamed_constructors_first: true
    unnecessary_await_in_return: true
    unnecessary_breaks: true
    unnecessary_lambdas: true
    unnecessary_late: true
    unnecessary_overrides: true
    unnecessary_parenthesis: true
    unnecessary_string_escapes: true
    unnecessary_string_interpolations: true
    unnecessary_this: true
    use_enums: true
    use_named_constants: true
    use_string_buffers: true
    use_super_parameters: true

    # ---- 以下与 formatter 冲突或噪声过大，显式关闭 ----
    require_trailing_commas: false
    prefer_expression_function_bodies: false
    cascade_invocations: false
    avoid_positional_boolean_parameters: false
    join_return_with_assignment: false
    diagnostic_describe_all_properties: false
    public_member_api_docs: false
    lines_longer_than_80_chars: false
    # `abstract final class X { static const ... }` 是本项目表达「常量命名空间」的
    # 标准手法（如 PfErrorCode）。它比散落的顶层常量更易搜索、更好控制可见性。
    avoid_classes_with_only_static_members: false
